#!/usr/bin/env bash

set -euf -o pipefail

HOST="i.jarv.org"
dir=$(dirname "$0")
cfg_dir="$dir/../cfg"

scp "$cfg_dir"/caddy/jarv.caddy root@$HOST:/var/opt/caddy/servers/.

ssh root@$HOST 'systemctl reload caddy'
